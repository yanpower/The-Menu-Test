global.config = false
global.popup = false


if(global.config == true)exit;
if(global.popup == true)exit;

button_list = [0, 1, 2]

index = 0;
button_max = array_length(button_list);
last_button = array_last(button_list);